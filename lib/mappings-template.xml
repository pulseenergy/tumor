<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="JavaScriptLibraryMappings"><% _.each(projects, function (project) { %>
    <file url="file://$PROJECT_DIR$/<%= project.name %>" libraries="{Node.js Core, <%= project.name %> node_modules}" /><% }); %>
  </component>
</project>
